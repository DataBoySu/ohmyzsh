<!--START_SECTION:navbar-->
<div align="center">
  <a href="../README.md">🇺🇸 English</a> | <a href="README.de.md">🇩🇪 Deutsch</a> | <a href="README.es.md">🇪🇸 Español</a> | <a href="README.fr.md">🇫🇷 Français</a> | <a href="README.hi.md">🇮🇳 हिंदी</a> | <a href="README.ja.md">🇯🇵 日本語</a> | <a href="README.ko.md">🇰🇷 한국어</a> | <a href="README.pt.md">🇵🇹 Português</a> | <a href="README.ru.md">🇷🇺 Русский</a> | <a href="README.zh.md">🇨🇳 中文</a>
</div>
<!--END_SECTION:navbar-->

<p align="center"><img src="https://ohmyzsh.s3.amazonaws.com/omz-ansi-github.png" alt="Oh My Zsh"></p>

Oh My Zsh — это открытый исходный код, сообщество, управляемый фреймворком для управления вашей [zsh](https://www.zsh.org/)
конфигурации.

Звучит скучно. Попробуем еще раз.

**Oh My Zsh не сделает вас 10x разработчиком...но вы можете почувствовать себя таковым.**

После установки ваш оболочка терминала станет темой для разговоров _или мы вернем вашу деньги!_ С каждым нажатием клавиши
в вашем командном приглашении вы будете использовать сотни мощных плагинов и красивых тем.
Странники будут подходить к вам в кафе и спрашивать, _"это потрясающе! вы какой-то гений?"_

В конце концов, вы начнете получать тот тип внимания, который вы всегда чувствовали, что заслуживаете. ...или, может быть, вы
используете время, которое вы экономите, чтобы начать чаще чистить зубы. 😬

Чтобы узнать больше, посетите [ohmyz.sh](https://ohmyz.sh), следуйте за [@ohmyzsh](https://x.com/ohmyzsh) в X (бывший
Twitter), и присоединяйтесь к нам на [Discord](https://discord.gg/ohmyzsh).

[![CI](https://github.com/ohmyzsh/ohmyzsh/workflows/CI/badge.svg)](https://github.com/ohmyzsh/ohmyzsh/actions?query=workflow%3ACI)
[![OpenSSF Best Practices](https://www.bestpractices.dev/projects/10713/badge)](https://www.bestpractices.dev/projects/10713)
[![X (formerly Twitter) Follow](https://img.shields.io/twitter/follow/ohmyzsh?label=%40ohmyzsh&logo=x&style=flat)](https://twitter.com/intent/follow?screen_name=ohmyzsh)
[![Mastodon Follow](https://img.shields.io/mastodon/follow/111169632522566717?label=%40ohmyzsh&domain=https%3A%2F%2Fmstdn.social&logo=mastodon&style=flat)](https://mstdn.social/@ohmyzsh)
[![Discord server](https://img.shields.io/discord/642496866407284746)](https://discord.gg/ohmyzsh)

<details>
<summary>Table of Contents</summary>

- [Getting Started](#getting-started)
  - [Operating System Compatibility](#operating-system-compatibility)
  - [Prerequisites](#prerequisites)
  - [Basic Installation](#basic-installation)
    - [Manual Inspection](#manual-inspection)
- [Using Oh My Zsh](#using-oh-my-zsh)
  - [Plugins](#plugins)
    - [Enabling Plugins](#enabling-plugins)
    - [Using Plugins](#using-plugins)
  - [Themes](#themes)
    - [Selecting A Theme](#selecting-a-theme)
  - [FAQ](#faq)
- [Advanced Topics](#advanced-topics)
  - [Advanced Installation](#advanced-installation)
    - [Custom Directory](#custom-directory)
    - [Unattended Install](#unattended-install)
    - [Installing From A Forked Repository](#installing-from-a-forked-repository)
    - [Manual Installation](#manual-installation)
  - [Installation Problems](#installation-problems)
  - [Custom Plugins And Themes](#custom-plugins-and-themes)
  - [Enable GNU ls In macOS And freeBSD Systems](#enable-gnu-ls-in-macos-and-freebsd-systems)
  - [Skip Aliases](#skip-aliases)
  - [Async git prompt](#async-git-prompt)
- [Getting Updates](#getting-updates)
  - [Updates Verbosity](#updates-verbosity)
  - [Manual Updates](#manual-updates)
- [Uninstalling Oh My Zsh](#uninstalling-oh-my-zsh)
- [How Do I Contribute To Oh My Zsh?](#how-do-i-contribute-to-oh-my-zsh)
  - [Do Not Send Us Themes](#do-not-send-us-themes)
- [Contributors](#contributors)
- [Follow Us](#follow-us)
- [Merchandise](#merchandise)
- [License](#license)
- [About Planet Argon](#about-planet-argon)

</details>

## Начало работы

### Совместимость с операционными системами

| ОС            | Статус |
| :------------- | :----: |
| Android        |   ✅   |
| freeBSD        |   ✅   |
| LCARS          |   🛸   |
| Linux          |   ✅   |
| macOS          |   ✅   |
| OS/2 Warp      |   ❌   |
| Windows (WSL2) |   ✅   |

### Предварительные требования

- [Zsh](https://www.zsh.org) должен быть установлен (версия 4.3.9 или более новая подойдет, но мы рекомендуем 5.0.8 и более новую). Если он не установлен заранее (выполните `zsh --version`, чтобы убедиться), проверьте следующие инструкции вики здесь:
  [Установка ZSH](https://github.com/ohmyzsh/ohmyzsh/wiki/Installing-ZSH)
- `curl` или `wget` должен быть установлен
- `git` должен быть установлен (рекомендуется версия 2.4.11 или выше)

### Базовая установка

Oh My Zsh устанавливается с помощью выполнения одного из следующих команд в вашем терминале. Вы можете установить это через командную строку с помощью `curl`, `wget` или другого подобного инструмента.

| Метод    | Команда                           |
| :-------- | : |
| **curl**  | `sh -c "$(curl -fsSL https://raw.githubusercontent.com/ohmyzsh/ohmyzsh/master/tools/install.sh)"` |
| **wget**  | `sh -c "$(wget -O- https://raw.githubusercontent.com/ohmyzsh/ohmyzsh/master/tools/install.sh)"`   |
| **fetch** | `sh -c "$(fetch -o - https://raw.githubusercontent.com/ohmyzsh/ohmyzsh/master/tools/install.sh)"` |

В качестве альтернативы, установщик также зеркалируется вне GitHub. Использование этого URL может быть необходимо, если вы находитесь в стране, такой как Китай или Индия (для определенных провайдеров), где блокируется `raw.githubusercontent.com`:

| Метод    | Команда |
| :-------- | : |
| **curl**  | `sh -c "$(curl -fsSL https://install.ohmyz.sh/)"` |
| **wget**  | `sh -c "$(wget -O- https://install.ohmyz.sh/)"`   |
| **fetch** | `sh -c "$(fetch -o - https://install.ohmyz.sh/)"` |

_Примечание: любой предыдущий `.zshrc` будет переименован в `.zshrc.pre-oh-my-zsh`. После установки вы можете переместить конфигурацию, которую хотите сохранить, в новый `.zshrc`._

#### Ручная проверка

Хорошей идеей является проверка скрипта установки из проектов, которые вы еще не знаете. Сделать это можно, сначала скачав скрипт установки, изучив его, чтобы убедиться, что всё выглядит нормально, а затем запустив его:

```sh
wget https://raw.githubusercontent.com/ohmyzsh/ohmyzsh/master/tools/install.sh
sh install.sh
```

Если приведенный выше URL завершится с тайм-аутом или иным образом завершится неудачно, вам может понадобиться заменить URL на
`https://install.ohmyz.sh`, чтобы получить скрипт.

## Использование Oh My Zsh

### Плагины

Oh My Zsh поставляется с огромным количеством плагинов, которые вы можете использовать. Вы можете посмотреть в директории
[plugins](https://github.com/ohmyzsh/ohmyzsh/tree/master/plugins) и/или
[wiki](https://github.com/ohmyzsh/ohmyzsh/wiki/Plugins), чтобы увидеть, что доступно на данный момент.

#### Включение плагинов

Как только вы найдете плагин (или несколько плагинов), которые хотите использовать с Oh My Zsh, вам нужно будет включить их в файл
`.zshrc`. Вы найдете файл zshrc в вашем каталоге `$HOME`. Откройте его в своем любимом текстовом редакторе
и вы увидите место, где можно перечислить все плагины, которые хотите загрузить.

```sh
vi ~/.zshrc
```

Например, это может начать выглядеть так:

```sh
plugins=(
  git
  bundler
  dotenv
  macos
  rake
  rbenv
  ruby
```

Обратите внимание, что плагины разделены пробелами (пробелы, табуляции, новые строки...). **Не используйте** запятые между ними, иначе это приведет к сбою.

#### Использование плагинов

Каждый встроенный плагин включает в себя **README**, документирующий его. Это README должно показывать псевдонимы (если плагин добавляет какие-либо) и дополнительные функции, включенные в этот конкретный плагин.

### Темы

Мы это признать. В ранние дни Oh My Zsh мы, возможно, были немного слишком увлечены темами. Сейчас у нас более чем сто пятьдесят тем, включённых в пакет. Большинство из них имеют
[скриншоты](https://github.com/ohmyzsh/ohmyzsh/wiki/Themes) на вики (Мы работаем над обновлением этого!).
Посмотрите их!

#### Выбор темы

Тема Robby — это тема по умолчанию. Она не самая изысканная. Она не самая простая. Это просто правильная тема (для него).

Как только вы найдете тему, которую хотите использовать, вам нужно будет отредактировать файл `~/.zshrc`. Вы увидите переменную окружения (все заглавные буквы) в ней, которая будет выглядеть примерно так:

```sh
ZSH_THEME="robbyrussell"
```

Чтобы использовать другой тем, просто измените значение, чтобы оно совпадало с названием вашей желаемой темы. Например:

```sh
ZSH_THEME="agnoster" # (this is one of the fancy ones)
# see https://github.com/ohmyzsh/ohmyzsh/wiki/Themes#agnoster
```

<!-- prettier-ignore-start -->

> [!NOTE]
> You will many times see screenshots for a zsh theme, and try it out, and find that it doesn't look the same for you.
>
> This is because many themes require installing a [Powerline Font](https://github.com/powerline/fonts) or a
> [Nerd Font](https://github.com/ryanoasis/nerd-fonts) in order to render properly. Without them, these themes
> will render weird prompt symbols. Check out
> [the FAQ](https://github.com/ohmyzsh/ohmyzsh/wiki/FAQ#i-have-a-weird-character-in-my-prompt) for more
> information.
>
> Also, beware that themes only control what your prompt looks like. This is, the text you see before or after
> your cursor, where you'll type your commands. Themes don't control things such as the colors of your
> terminal window (known as _color scheme_) or the font of your terminal. These are settings that you can
> change in your terminal emulator. For more information, see
> [what is a zsh theme](https://github.com/ohmyzsh/ohmyzsh/wiki/FAQ#what-is-a-zsh-theme).

<!-- prettier-ignore-end -->

Open up a new terminal window and your prompt should look something like this:

![Agnoster theme](https://cloud.githubusercontent.com/assets/2618447/6316862/70f58fb6-ba03-11e4-82c9-c083bf9a6574.png)

In case you did not find a suitable theme for your needs, please have a look at the wiki for
[more of them](https://github.com/ohmyzsh/ohmyzsh/wiki/External-themes).

If you're feeling feisty, you can let the computer select one randomly for you each time you open a new
terminal window.

```sh
ZSH_THEME="random" # (...please let it be pie... please be some pie..)
```

И если вы хотите выбрать случайную тему из списка ваших любимых тем:

```sh
ZSH_THEME_RANDOM_CANDIDATES=(
  "robbyrussell"
  "agnoster"
```

Если вы знаете только те темы, которые не нравятся, вы можете добавить их аналогичным образом в список игнорируемых:

```sh
ZSH_THEME_RANDOM_IGNORED=(pygmalion tjkirch_mod)
```

### ЧАСТО ЗАДАВАЕМЫЕ ВОПРОСЫ

Если у вас есть дополнительные вопросы или проблемы, вы можете найти решение в нашем
[ЧАСТО ЗАДАВАЕМЫЕ ВОПРОСЫ](https://github.com/ohmyzsh/ohmyzsh/wiki/FAQ).

## Расширенные темы

Если вы того типа, кто любит пачкать руки, эти разделы могут заинтересовать.

### Расширенная установка

Некоторые пользователи могут захотеть вручную установить Oh My Zsh, или изменить стандартный путь или другие настройки, которые принимает установщик (эти настройки также описаны вверху скрипта установки).

#### Пользовательская директория

По умолчанию расположение `~/.oh-my-zsh` (скрыто в вашем домашнем каталоге, вы можете получить доступ к нему с помощью
`cd ~/.oh-my-zsh`)

Если вы хотите изменить директорию установки с помощью переменной окружения `ZSH`, либо запустив
`export ZSH=/your/path` до установки, либо установив её до окончания процесса установки следующим образом:

```sh
ZSH="$HOME/.dotfiles/oh-my-zsh" sh install.sh
```

#### Безопасная установка

Если вы запускаете скрипт установки Oh My Zsh в составе автоматической установки, вы можете передать флаг
`--unattended` скрипту `install.sh`. Это приведет к тому, что не будет предприниматься попыток изменить
по умолчанию оболочку, а также не будет запускаться `zsh` после завершения установки.

```sh
sh -c "$(curl -fsSL https://raw.githubusercontent.com/ohmyzsh/ohmyzsh/master/tools/install.sh)" "" --unattended
```

Если вы находитесь в Китае, Индии или в другой стране, блокирующей `raw.githubusercontent.com`, вам, возможно, придется заменить URL на `https://install.ohmyz.sh`, чтобы установить его.

#### Установка из ветвленного репозитория

Скрипт установки также принимает эти переменные, чтобы разрешить установку из другого репозитория:

- `REPO` (по умолчанию: `ohmyzsh/ohmyzsh`): это должно быть в формате `владелец/репозиторий`. Если вы установите эту переменную,
  установщик будет искать репозиторий по адресу `https://github.com/{владелец}/{репозиторий}`.

- `REMOTE` (по умолчанию: `https://github.com/${REPO}.git`): это полный URL для клонирования git-репозитория. Вы
  можете использовать это настройку, если хотите установить из ветвления, которое не находится на GitHub (GitLab, Bitbucket...) или если
  вы хотите клонировать через SSH вместо HTTPS (`git@github.com:пользователь/проект.git`).

  _ПРИМЕЧАНИЕ: это несовместимо с установкой переменной `REPO`. Эта настройка будет иметь приоритет._

- `BRANCH` (по умолчанию: `master`): вы можете использовать эту настройку, если хотите изменить ветку по умолчанию, которая будет
  проверена при клонировании репозитория. Это может быть полезно для тестирования Pull Request или если вы хотите использовать
  ветку, отличную от `master`.

Например:

```sh
REPO=apjanke/oh-my-zsh BRANCH=edge sh install.sh
```

#### Ручная установка

##### 1. Clone The Repository <!-- omit in toc -->

```sh
git clone https://github.com/ohmyzsh/ohmyzsh.git ~/.oh-my-zsh
```

##### 2. _Optionally_, Backup Your Existing `~/.zshrc` File <!-- omit in toc -->

```sh
cp ~/.zshrc ~/.zshrc.orig
```

##### 3. Create A New Zsh Configuration File <!-- omit in toc -->

Вы можете создать новый файл конфигурации zsh, скопировав шаблон, который мы включили для вас.

```sh
cp ~/.oh-my-zsh/templates/zshrc.zsh-template ~/.zshrc
```

##### 4. Change Your Default Shell <!-- omit in toc -->

```sh
chsh -s $(which zsh)
```

Вам необходимо выйти из своей пользовательской сессии и снова войти, чтобы увидеть это изменение.

##### 5. Initialize Your New Zsh Configuration <!-- omit in toc -->

После того как вы откроете новое окно терминала, оно должно загрузить zsh с настройками Oh My Zsh.

### Проблемы установки

Если у вас возникли проблемы с установкой, вот несколько распространенных решений.

- Возможно, вам потребуется изменить ваш `PATH` в `~/.zshrc`, если вы не можете найти некоторые команды после
  перехода на `oh-my-zsh`.
- Если вы установили вручную или изменили расположение установки, проверьте переменную окружения `ZSH` в
  `~/.zshrc`.

### Пользовательские плагины и темы

Если вы хотите переопределить любое из поведений по умолчанию, просто добавьте новый файл (оканчивающийся на `.zsh`) в директорию `custom/`.

Если у вас есть много функций, которые хорошо сочетаются между собой, вы можете поместить их в файл `XYZ.plugin.zsh` в директории `custom/plugins/`, а затем включить этот плагин.

Если вы хотите переопределить функциональность плагина, поставляемого с Oh My Zsh, создайте плагин с тем же именем в директории `custom/plugins/`, и он будет загружен вместо того, что находится в `plugins/`.

### Включение GNU ls в системах macOS и freeBSD

<a name="enable-gnu-ls"></a>

По умолчанию в Oh My Zsh используется BSD `ls` в системах macOS и FreeBSD. Если установлен GNU `ls`
(в виде команды `gls`), вы можете выбрать его вместо этого. Для этого вы можете использовать конфигурацию на основе zstyle до
подключения файла `oh-my-zsh.sh`:

```zsh
zstyle ':omz:lib:theme-and-appearance' gnu-ls yes
```

Примечание: это несовместимо с `DISABLE_LS_COLORS=true`

### Пропуск псевдонимов

<a name="remove-directories-aliases"></a>

Если вы хотите пропустить стандартные псевдонимы Oh My Zsh (те, которые определены в файлах `lib/*`) или псевдонимы плагинов, вы можете использовать
нижеуказанные настройки в файле `~/.zshrc`, **до загрузки Oh My Zsh**. Обратите внимание, что существует множество различных
способов пропуска псевдонимов, в зависимости от ваших потребностей.

```sh
# Skip all aliases, in lib files and enabled plugins
zstyle ':omz:*' aliases no

# Skip all aliases in lib files
zstyle ':omz:lib:*' aliases no
# Skip only aliases defined in the directories.zsh lib file
zstyle ':omz:lib:directories' aliases no

# Skip all plugin aliases
zstyle ':omz:plugins:*' aliases no
# Skip only the aliases from the git plugin
zstyle ':omz:plugins:git' aliases no
```

Вы можете комбинировать их иными способами, учитывая, что более конкретные области имеют приоритет:

```sh
# Skip all plugin aliases, except for the git plugin
zstyle ':omz:plugins:*' aliases no
zstyle ':omz:plugins:git' aliases yes
```

Предыдущая версия этой функции использовала настройку ниже, которая была удалена:

```sh
zstyle ':omz:directories' aliases no
```

Вместо этого теперь можно использовать следующее:

```sh
zstyle ':omz:lib:directories' aliases no
```

#### Notice <!-- omit in toc -->

> Эта функция в настоящее время находится на стадии тестирования и может быть изменена в будущем. Она также не
> совместима с менеджерами плагинов, такими как zpm или zinit, которые не загружают скрипт инициализации
> (`oh-my-zsh.sh`), где реализована эта функция.

> Она также не распознает "алиасы", определенные как функции. Примеры таких функций — `gccd`,
> `ggf` или `ggl` из плагина git.

### Async git prompt

Async prompt functions are an experimental feature (included on April 3, 2024) that allows Oh My Zsh to render
prompt information asynchronously. This can improve prompt rendering performance, but it might not work well
with some setups. We hope that's not an issue, but if you're seeing problems with this new feature, you can
turn it off by setting the following in your .zshrc file, before Oh My Zsh is sourced:

```sh
zstyle ':omz:alpha:lib:git' async-prompt no
```

Если ваша проблема в том, что приглашение git внезапно перестало отображаться, вы можете попробовать принудительно включить его, установив следующую конфигурацию до того, как будет загружен `oh-my-zsh.sh`. Если это не сработает, пожалуйста, откройте проблему, указав свой случай.

```sh
zstyle ':omz:alpha:lib:git' async-prompt force
```

## Получение обновлений

По умолчанию вы будете получать уведомления о проверке обновлений каждые 2 недели. Вы можете выбрать другие режимы обновления, добавив строку в ваш файл `~/.zshrc`, **до того, как будет загружен Oh My Zsh**:

1. Автоматическое обновление без запроса подтверждения:

```sh
   zstyle ':omz:update' mode auto
   ```

2. Просто предлагайте напоминание каждые несколько дней, если доступны обновления:

```sh
   zstyle ':omz:update' mode reminder
   ```

3. Чтобы полностью отключить автоматические обновления:

```sh
   zstyle ':omz:update' mode disabled
   ```

ПРИМЕЧАНИЕ: вы можете управлять частотой, с которой Oh My Zsh проверяет обновления, с помощью следующего параметра:

```sh
# This will check for updates every 7 days
zstyle ':omz:update' frequency 7
# This will check for updates every time you open the terminal (not recommended)
zstyle ':omz:update' frequency 0
```

### Уровень детализации обновлений

Вы также можете ограничить уровень детализации обновлений с помощью следующих настроек:

```sh
zstyle ':omz:update' verbose default # default update prompt

zstyle ':omz:update' verbose minimal # only few lines

zstyle ':omz:update' verbose silent # only errors
```

### Ручные обновления

Если вы хотите обновить в любое время (возможно, кто-то только что выпустил новый плагин, и вы не хотите ждать неделю?), вам просто нужно запустить:

```sh
omz update
```

> [!NOTE]
> If you want to automate this process in a script, you should call directly the `upgrade` script, like this:
>
>

```sh

> $ZSH/tools/upgrade.sh
> ```

>
> См. больше вариантов в [FAQ: Как обновить Oh My Zsh?](https://github.com/ohmyzsh/ohmyzsh/wiki/FAQ#how-do-i-update-oh-my-zsh).
>
> **ИСПОЛЬЗОВАНИЕ `omz update --unattended` БЫЛО УДАЛЕНО, ПОТОМУ ЧТО ОНО ИМЕЕТ ПОБОЧНЫЕ ЭФФЕКТЫ**.

Магия! 🎉

## Удаление Oh My Zsh

Oh My Zsh не подходит всем. Мы будем скучать по вам, но хотим, чтобы это было легкое расставание.

Если вы хотите удалить `oh-my-zsh`, просто запустите `uninstall_oh_my_zsh` из командной строки. Он удалит
себя и отменит ваш предыдущий `bash` или `zsh` конфигурацию.

## Как я могу внести свой вклад в Oh My Zsh?

Перед тем как присоединиться к нашему замечательному сообществу, пожалуйста, прочитайте [кодекс поведения](../CODE_OF_CONDUCT.md).

Я вовсе не эксперт по [Zsh](https://www.zsh.org/), и подозреваю, что есть множество способов улучшить проект – если у вас есть идеи, как сделать настройку проще для поддержания (и быстрее), не стесняйтесь форкнуть репозиторий и отправить пулл-реквесты!

Нам также нужны люди, которые будут тестировать пулл-реквесты. Поэтому посмотрите [открытые задачи](https://github.com/ohmyzsh/ohmyzsh/issues) и помогайте там, где сможете.

Для получения дополнительной информации см. [Contributing](../CONTRIBUTING.md).

### Не отправляйте нам темы

На данный момент у нас (более чем) достаточно тем. Пожалуйста, добавьте свою тему на страницу
[внешних тем](https://github.com/ohmyzsh/ohmyzsh/wiki/External-themes) вики.

## Спонсоры

Oh My Zsh имеет живое сообщество довольных пользователей и приятных участников. Без всех времени и помощи
от наших участников, это не было бы таким потрясающим.

Большое спасибо!

<a href="https://github.com/ohmyzsh/ohmyzsh/graphs/contributors">
  <img src="https://contrib.rocks/image?repo=ohmyzsh/ohmyzsh" width="100%"/>
</a>

## Следите за нами

Мы в социальных сетях:

- [@ohmyzsh](https://x.com/ohmyzsh) на X (ранее Twitter). Вы должны подписаться.
- [Facebook](https://www.facebook.com/Oh-My-Zsh-296616263819290/) нажмите на нас.
- [Instagram](https://www.instagram.com/_ohmyzsh/) упомяните нас в вашем посте с Oh My Zsh!
- [Discord](https://discord.gg/ohmyzsh) чтобы поговорить с нами!

## Товары

У нас есть
[стикеры, футболки и кружки с кофе](https://commitgoods.com/collections/oh-my-zsh?utm_source=github)
, чтобы вы могли гордиться своей любовью к Oh My Zsh. Опять же, вы станете тем, о ком будут говорить!

## Лицензия

Oh My Zsh распространяется под лицензией [MIT](../LICENSE.txt).

## О Planet Argon

![Planet Argon](https://pa-github-assets.s3.amazonaws.com/PARGON_logo_digital_COL-small.jpg)

Oh My Zsh was started by the team at [Planet Argon](https://www.planetargon.com/?utm_source=github), a
[Ruby on Rails development agency](https://www.planetargon.com/services/ruby-on-rails-development?utm_source=github).
Check out our [other open source projects](https://www.planetargon.com/open-source?utm_source=github).

